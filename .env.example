# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Set to "PRODUCTION" to require users to provide their own Firecrawl API key
# Leave empty or set to anything else to use the server FIRECRAWL_API_KEY
NEXT_PUBLIC_APP_MODE=PRODUCTION

# Firecrawl API - Get your key at https://www.firecrawl.dev/app/api-keys
# Only used when NEXT_PUBLIC_APP_MODE is not "PRODUCTION"
FIRECRAWL_API_KEY=
